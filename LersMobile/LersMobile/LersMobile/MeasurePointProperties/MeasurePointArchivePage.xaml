<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"             
             x:Class="LersMobile.MeasurePointProperties.MeasurePointArchivePage"      
			 xmlns:local="clr-namespace:LersMobile"
             xmlns:dg="clr-namespace:Xamarin.Forms.DataGrid;assembly=Xamarin.Forms.DataGrid">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Filter"
					 Icon="filter.png"
					 Clicked="Filter_ToolbarItem_Clicked" />
	</ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Picker IsVisible="False" x:Name="dataTypePicker" SelectedIndexChanged="OnDataTypeSelected"/>
<!--
            <dg:DataGrid
                         x:Name="dataGrid"
                         ItemsSource="{Binding Data}" 
                         RowHeight="70" HeaderHeight="50" BorderColor="#CCCCCC"
                         HeaderBackground="#E0E6F8"
                         IsRefreshing="{Binding IsBusy}">
            </dg:DataGrid>
            -->

            <ListView ItemsSource="{Binding Data}">
				<ListView.Header>
					<Grid >
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>
						<Label Text="Дата" Grid.Column="0" Grid.Row="0" />
						<Label Text="Quantity" Grid.Column="1" Grid.Row="0"/>
					</Grid>
				</ListView.Header>
				
				<ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*"></ColumnDefinition>
									<ColumnDefinition Width="Auto"></ColumnDefinition>
								</Grid.ColumnDefinitions>

								<Grid.RowDefinitions>
									<RowDefinition Height="*"></RowDefinition>
									<RowDefinition Height="*"></RowDefinition>
								</Grid.RowDefinitions>
								
							</Grid>

							<Label Text="{Binding DateTime}" />


							<!--
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding DateTime}"/>
                                <Label Text="{Binding M_in, StringFormat='{0:0.00}'}"/>
                                <Label Text="{Binding M_out, StringFormat='{0:0.00}'}"/>
                            </StackLayout>
							-->
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>