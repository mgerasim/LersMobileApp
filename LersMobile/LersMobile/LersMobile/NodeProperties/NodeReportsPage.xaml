<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:LersMobile.NodeProperties.ViewModels.Converters"
             x:Class="LersMobile.NodeProperties.NodeReportsPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ReportTitleConverter x:Key="converter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Reports}" SelectedItem="{Binding SelectedReport}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding Path=. , Converter={StaticResource converter}}"/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>                
            </ListView>
            <Picker ItemsSource="{Binding Reports}"
                    ItemDisplayBinding="{Binding Path=Report , Converter={StaticResource converter}}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>